# 服务端读取（不要把真实密钥提交到仓库）
# Provider 档案里填 api_key_env 为下列变量名之一

# 后端监听端口（默认 8787）。前端 Vite 开发代理会读同一变量，请与 server 保持一致。
# PORT=8787
# 本机开发默认 127.0.0.1；局域网其它设备访问 API 时请改为 0.0.0.0
# HOST=127.0.0.1
# HOST=0.0.0.0
# 同上时，前端 Vite 也会监听局域网（web/vite.config.ts 已关联 HOST）；若只想暴露前端可单独设：VITE_DEV_HOST=true
#
# 浏览器从「非本机」打开前端时，必须把前端来源写进 CORS（逗号分隔，勿有空格）
# 例：本机 IP 为 192.168.1.8、Vite 端口 5173 时：
# CORS_ORIGINS=http://192.168.1.8:5173,http://127.0.0.1:5173,http://localhost:5173

OPENAI_API_KEY=
# 阿里云 DashScope 兼容 OpenAI 时可使用，例如 base_url=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_API_KEY=
# 火山引擎方舟等 OpenAI 兼容网关
VOLCENGINE_ARK_API_KEY=

# 可选：SQLite 路径（默认 server/data/app.db）
# SQLITE_PATH=./server/data/app.db

# 可选：托管模式「测试集根目录」（相对进程工作目录或绝对路径）。优先级高于 image-tester.config.json。
# IMAGE_TESTER_SUITE_ROOT=./data/test-suites

# 可选：自定义 JSON 配置文件路径（绝对路径，或相对当前工作目录）。默认读取项目根目录 image-tester.config.json。
# IMAGE_TESTER_CONFIG=./image-tester.config.json
