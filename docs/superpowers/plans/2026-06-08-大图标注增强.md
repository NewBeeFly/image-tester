# 大图标注浮窗增强实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在大图标注浮窗（`CaseAnnotator`）中支持图片缩放/平移/重置、上一张/下一张翻页（带自动保存）、并把滚轮"在图片区缩放、不影响列表"。

**Architecture:** 新增独立的 `ImageViewer` 组件（自管 scale/tx/ty/status，forwardRef 暴露 `resetView`），在 `CaseAnnotator` 内替换原 `<img>` 容器；`App.tsx` 增 `navigateCase(direction)` 包装自动保存与列表刷新。完全前端，不动后端 API、不引入新依赖。

**Tech Stack:** React 19 + TypeScript + Vite；CSS（沿用 `App.css` 变量）；不引第三方缩放库。

---

## 项目无单测基建说明

`web/package.json` 没有 test/vitest 脚本。沿用"手动浏览器验收 + `tsc -b` 通过"作为每任务的硬性收尾。所有 UI 行为通过 `vite dev` + 浏览器手工验证（spec §6）。

---

### Task 1: 新增 `ImageViewer` 组件（缩放/平移/工具条骨架）

**Files:**
- Create: `web/src/components/ImageViewer.tsx`

- [ ] **Step 1: 创建文件并写入基础组件**

```tsx
import {
  forwardRef,
  useCallback,
  useEffect,
  useImperativeHandle,
  useRef,
  useState,
} from 'react'

export type ImageViewerHandle = {
  resetView: () => void
}

type Status = 'idle' | 'loading' | 'error'

type Props = {
  src: string
  alt: string
  hasPrev?: boolean
  hasNext?: boolean
  onPrev?: () => void
  onNext?: () => void
}

const MIN_SCALE = 0.25
const MAX_SCALE = 8
const STEP = 1.25

function clamp(v: number, lo: number, hi: number) {
  return Math.max(lo, Math.min(hi, v))
}

export const ImageViewer = forwardRef<ImageViewerHandle, Props>(function ImageViewer(
  { src, alt, hasPrev, hasNext, onPrev, onNext },
  ref,
) {
  const stageRef = useRef<HTMLDivElement | null>(null)
  const imgRef = useRef<HTMLImageElement | null>(null)
  const [scale, setScale] = useState(1)
  const [tx, setTx] = useState(0)
  const [ty, setTy] = useState(0)
  const [status, setStatus] = useState<Status>('loading')
  const [natural, setNatural] = useState<{ w: number; h: number } | null>(null)
  const [retryToken, setRetryToken] = useState(0)

  // 关闭所有内部交互时暴露给父级：缩放重置、平移归零
  useImperativeHandle(ref, () => ({
    resetView: () => {
      setScale(1)
      setTx(0)
      setTy(0)
    },
  }))

  // src 变化时进入 loading
  useEffect(() => {
    setStatus('loading')
  }, [src, retryToken])

  const handleLoad = useCallback(() => {
    const img = imgRef.current
    if (img) setNatural({ w: img.naturalWidth, h: img.naturalHeight })
    setStatus('idle')
  }, [])

  const handleError = useCallback(() => {
    setStatus('error')
  }, [])

  // 工具条：放大
  const zoomIn = useCallback(() => {
    setScale((s) => clamp(s * STEP, MIN_SCALE, MAX_SCALE))
  }, [])

  // 工具条：缩小
  const zoomOut = useCallback(() => {
    setScale((s) => clamp(s / STEP, MIN_SCALE, MAX_SCALE))
  }, [])

  // 工具条：点击倍率 — 100% ↔ 适应
  const toggleFit100 = useCallback(() => {
    setScale((s) => (s === 1 ? 1 : 1))
  }, [])

  // 工具条：适应
  const fit = useCallback(() => {
    setScale(1)
    setTx(0)
    setTy(0)
  }, [])

  return (
    <div className="ivStage" ref={stageRef}>
      <div
        className="ivImgWrap"
        style={{ transform: `translate(${tx}px, ${ty}px) scale(${scale})` }}
      >
        <img
          ref={imgRef}
          className="ivImg"
          src={status === 'error' ? '' : `${src}${retryToken ? `?retry=${retryToken}` : ''}`}
          alt={alt}
          onLoad={handleLoad}
          onError={handleError}
          draggable={false}
        />
      </div>

      {hasPrev ? (
        <button
          type="button"
          className="ivNav ivNavPrev"
          onClick={onPrev}
          aria-label="上一张"
        >
          ‹
        </button>
      ) : null}
      {hasNext ? (
        <button
          type="button"
          className="ivNav ivNavNext"
          onClick={onNext}
          aria-label="下一张"
        >
          ›
        </button>
      ) : null}

      <div className="ivToolbar">
        <button type="button" onClick={zoomOut} aria-label="缩小">−</button>
        <button type="button" onClick={toggleFit100}>
          {Math.round(scale * 100)}%
        </button>
        <button type="button" onClick={zoomIn} aria-label="放大">+</button>
        <button type="button" onClick={fit}>适应</button>
      </div>

      {status === 'loading' ? <div className="ivSpinner">加载中…</div> : null}
      {status === 'error' ? (
        <div className="ivError">
          <span>加载失败</span>
          <button type="button" onClick={() => setRetryToken((n) => n + 1)}>
            重试
          </button>
        </div>
      ) : null}
    </div>
  )
})
```

- [ ] **Step 2: 类型检查通过**

Run: `cd web && npx tsc -b`
Expected: 0 errors。

- [ ] **Step 3: 提交**

```bash
git add web/src/components/ImageViewer.tsx
git commit -m "feat(web): 新增 ImageViewer 组件（缩放/翻页/工具条骨架）"
```

---

### Task 2: 接入滚轮缩放（以光标为锚点）

**Files:**
- Modify: `web/src/components/ImageViewer.tsx`

- [ ] **Step 1: 替换组件顶部的 useCallback 块，添加 wheel/pointer 处理**

将 Task 1 中 `const fit = useCallback(...)` 之后整体替换为以下（保留前面所有 import/状态/ref）：

```tsx
  // 工具条：适应
  const fit = useCallback(() => {
    setScale(1)
    setTx(0)
    setTy(0)
  }, [])

  // 滚轮缩放：仅在 stage 上拦截，preventDefault 防止外层列表滚动
  const handleWheel = useCallback((e: React.WheelEvent<HTMLDivElement>) => {
    e.preventDefault()
    const stage = stageRef.current
    if (!stage) return
    const rect = stage.getBoundingClientRect()
    // wrap 中心 = stage 中心；transform-origin 也在 wrap 中心
    const centerX = rect.width / 2
    const centerY = rect.height / 2
    // 光标在「wrap 坐标系」中的位置（已考虑 tx/ty 平移）
    const cursorOnWrapX = e.clientX - rect.left - centerX - tx
    const cursorOnWrapY = e.clientY - rect.top - centerY - ty
    const factor = Math.exp(-e.deltaY * 0.0015)
    setScale((prevScale) => {
      const newScale = clamp(prevScale * factor, MIN_SCALE, MAX_SCALE)
      const ratio = newScale / prevScale
      // 保持光标下像素不动：newCursorOnWrap = cursorOnWrap / ratio
      // 之后位置 = center + (newCursorOnWrap * ratio) ... 简化见下
      setTx((prevTx) => {
        const newCursorX = cursorOnWrapX / ratio
        return centerX + newCursorX * ratio - (e.clientX - rect.left - centerX)
      })
      setTy((prevTy) => {
        const newCursorY = cursorOnWrapY / ratio
        return centerY + newCursorY * ratio - (e.clientY - rect.top - centerY)
      })
      return newScale
    })
  }, [tx, ty])

  // 拖动：仅记录拖动起点，松手前持续更新 tx/ty
  const dragRef = useRef<{ active: boolean; startX: number; startY: number; baseTx: number; baseTy: number }>({
    active: false,
    startX: 0,
    startY: 0,
    baseTx: 0,
    baseTy: 0,
  })

  const handlePointerDown = useCallback((e: React.PointerEvent<HTMLDivElement>) => {
    if (e.button !== 0) return
    ;(e.currentTarget as HTMLDivElement).setPointerCapture(e.pointerId)
    dragRef.current = {
      active: true,
      startX: e.clientX,
      startY: e.clientY,
      baseTx: tx,
      baseTy: ty,
    }
  }, [tx, ty])

  const handlePointerMove = useCallback((e: React.PointerEvent<HTMLDivElement>) => {
    if (!dragRef.current.active) return
    setTx(dragRef.current.baseTx + (e.clientX - dragRef.current.startX))
    setTy(dragRef.current.baseTy + (e.clientY - dragRef.current.startY))
  }, [])

  const handlePointerUp = useCallback((e: React.PointerEvent<HTMLDivElement>) => {
    if (!dragRef.current.active) return
    ;(e.currentTarget as HTMLDivElement).releasePointerCapture(e.pointerId)
    dragRef.current.active = false
  }, [])
```

- [ ] **Step 2: 在 stage `<div>` 上挂载 wheel/pointer 事件**

把：
```tsx
    <div className="ivStage" ref={stageRef}>
```
改为：
```tsx
    <div
      className="ivStage"
      ref={stageRef}
      onWheel={handleWheel}
      onPointerDown={handlePointerDown}
      onPointerMove={handlePointerMove}
      onPointerUp={handlePointerUp}
      onPointerCancel={handlePointerUp}
    >
```

- [ ] **Step 3: 类型检查通过**

Run: `cd web && npx tsc -b`
Expected: 0 errors。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/ImageViewer.tsx
git commit -m "feat(web): ImageViewer 滚轮缩放（光标锚点）+ 拖动平移"
```

---

### Task 3: 添加 `ImageViewer` 样式

**Files:**
- Modify: `web/src/App.css`（在文件末尾追加）

- [ ] **Step 1: 追加样式块**

将以下内容追加到 `web/src/App.css` 文件末尾（保持其它部分不变）：

```css
/* ===== ImageViewer（大图标注图片查看器） ===== */
.ivStage {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  cursor: grab;
  touch-action: none;
  background: #0f172a;
  border-radius: 10px;
  user-select: none;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ivStage:active {
  cursor: grabbing;
}

.ivImgWrap {
  position: relative;
  transform-origin: center center;
  will-change: transform;
  pointer-events: none;
  display: inline-block;
  line-height: 0;
}

.ivImg {
  display: block;
  pointer-events: none;
  user-select: none;
  -webkit-user-drag: none;
  max-width: none;
  max-height: none;
}

.ivToolbar {
  position: absolute;
  right: 12px;
  bottom: 12px;
  display: flex;
  gap: 4px;
  background: rgba(15, 23, 42, 0.78);
  border-radius: 8px;
  padding: 4px;
  color: #fff;
  z-index: 2;
}

.ivToolbar button {
  background: transparent;
  color: inherit;
  border: 0;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  min-width: 32px;
}

.ivToolbar button:hover {
  background: rgba(255, 255, 255, 0.12);
}

.ivNav {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: rgba(15, 23, 42, 0.7);
  color: #fff;
  border: 0;
  font-size: 22px;
  line-height: 1;
  cursor: pointer;
  opacity: 0;
  transition: opacity 0.15s;
  z-index: 2;
}

.ivStage:hover .ivNav {
  opacity: 0.9;
}

.ivNav:hover {
  background: rgba(15, 23, 42, 0.92);
}

.ivNavPrev {
  left: 12px;
}

.ivNavNext {
  right: 12px;
}

.ivSpinner {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(15, 23, 42, 0.4);
  color: #fff;
  font-size: 12px;
  z-index: 1;
}

.ivError {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  background: rgba(15, 23, 42, 0.6);
  color: #fff;
  font-size: 13px;
  z-index: 1;
}

.ivError button {
  background: rgba(255, 255, 255, 0.14);
  color: #fff;
  border: 0;
  padding: 4px 10px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
}
```

- [ ] **Step 2: 构建通过**

Run: `cd web && npx tsc -b && npx vite build`
Expected: 0 errors, build 成功。

- [ ] **Step 3: 提交**

```bash
git add web/src/App.css
git commit -m "feat(web): ImageViewer 样式（stage/工具条/翻页/加载/失败）"
```

---

### Task 4: 改造 `CaseAnnotator` 接入 `ImageViewer`

**Files:**
- Modify: `web/src/components/CaseAnnotator.tsx`

- [ ] **Step 1: 顶部 import 改写**

把：
```tsx
import { useEffect, useState } from 'react'
import { createPortal } from 'react-dom'
import { imageUrl, type TestCase } from '../api'
import { AssertionBuilder, type SchemaFieldOption } from './AssertionBuilder'
import { VariableBuilder } from './VariableBuilder'
import type { SuiteVarValueHint } from './SuiteVarListBuilder'
```
改为：
```tsx
import { useEffect, useRef, useState } from 'react'
import { createPortal } from 'react-dom'
import { imageUrl, type TestCase } from '../api'
import { AssertionBuilder, type SchemaFieldOption } from './AssertionBuilder'
import { VariableBuilder } from './VariableBuilder'
import type { SuiteVarValueHint } from './SuiteVarListBuilder'
import { ImageViewer, type ImageViewerHandle } from './ImageViewer'
```

- [ ] **Step 2: 扩展 props 类型**

把：
```tsx
export function CaseAnnotator(props: {
  open: boolean
  suiteId: number
  testCase: TestCase
  schemaFields: SchemaFieldOption[]
  /** 测试集声明的变量名列表，供变量编辑下拉候选 & 断言"引用变量"候选 */
  suiteDefinedVarKeys: string[]
  /** 测试集变量类型与 array 枚举，供变量值多选 */
  suiteVarHints: Record<string, SuiteVarValueHint>
  onClose: () => void
  onSave: (payload: {
    relative_image_path: string
    variables_json: string
    assertions_override_json: string
  }) => Promise<void> | void
}) {
  const { open, suiteId, testCase, schemaFields, suiteDefinedVarKeys, suiteVarHints, onClose, onSave } = props
```
改为：
```tsx
export function CaseAnnotator(props: {
  open: boolean
  suiteId: number
  testCase: TestCase
  schemaFields: SchemaFieldOption[]
  /** 测试集声明的变量名列表，供变量编辑下拉候选 & 断言"引用变量"候选 */
  suiteDefinedVarKeys: string[]
  /** 测试集变量类型与 array 枚举，供变量值多选 */
  suiteVarHints: Record<string, SuiteVarValueHint>
  /** 当前测试集下全部用例的可见顺序 */
  caseList: TestCase[]
  /** testCase 在 caseList 中的下标；找不到时为 -1 */
  currentIndex: number
  /** 翻页：父级负责保存 + 切换 id */
  onRequestNavigate: (direction: -1 | 1) => Promise<void> | void
  /** 把"草稿是否改动"上抛给父级（父级决定是否在翻页前自动保存） */
  onDirtyChange?: (dirty: boolean) => void
  onClose: () => void
  onSave: (payload: {
    relative_image_path: string
    variables_json: string
    assertions_override_json: string
  }) => Promise<void> | void
}) {
  const {
    open,
    suiteId,
    testCase,
    schemaFields,
    suiteDefinedVarKeys,
    suiteVarHints,
    caseList,
    currentIndex,
    onRequestNavigate,
    onDirtyChange,
    onClose,
    onSave,
  } = props
```

- [ ] **Step 3: 引入 ref 与 dirty 跟踪**

把：
```tsx
  const [relativePath, setRelativePath] = useState(testCase.relative_image_path)
  const [variablesJson, setVariablesJson] = useState('')
  const [assertionsJson, setAssertionsJson] = useState(testCase.assertions_override_json ?? '')
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState<string | null>(null)
```
改为：
```tsx
  const [relativePath, setRelativePath] = useState(testCase.relative_image_path)
  const [variablesJson, setVariablesJson] = useState('')
  const [assertionsJson, setAssertionsJson] = useState(testCase.assertions_override_json ?? '')
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const ivRef = useRef<ImageViewerHandle | null>(null)
  const lastSyncedCaseIdRef = useRef<number | null>(null)
  const isInternalChangeRef = useRef(false)
```

- [ ] **Step 4: 在 useEffect 中重置 ImageViewer 并把 dirty 上抛**

把：
```tsx
  // 初始化：当弹窗打开时，合并测试集默认值 + 用例已有变量
  useEffect(() => {
    if (!open) return
    setRelativePath(testCase.relative_image_path)
    setAssertionsJson(testCase.assertions_override_json ?? '')
    setError(null)
    const suiteDefaults = buildDefaultVariablesJson(suiteDefinedVarKeys, suiteVarHints)
    const merged = mergeVariablesJson(testCase.variables_json || '{}', suiteDefaults)
    setVariablesJson(merged)
  }, [open, testCase.id])
```
改为：
```tsx
  // 初始化：当弹窗打开时，合并测试集默认值 + 用例已有变量
  useEffect(() => {
    if (!open) return
    setRelativePath(testCase.relative_image_path)
    setAssertionsJson(testCase.assertions_override_json ?? '')
    setError(null)
    const suiteDefaults = buildDefaultVariablesJson(suiteDefinedVarKeys, suiteVarHints)
    const merged = mergeVariablesJson(testCase.variables_json || '{}', suiteDefaults)
    setVariablesJson(merged)
    // 重置图片查看器 + 关闭 dirty 标记
    ivRef.current?.resetView()
    isInternalChangeRef.current = false
    onDirtyChange?.(false)
  }, [open, testCase.id])
```

- [ ] **Step 5: 替换图片渲染为 ImageViewer**

把：
```tsx
        <div className="annotatorBody">
          <div className="annotatorImage">
            <img
              src={imageUrl(suiteId, testCase.relative_image_path)}
              alt={testCase.relative_image_path}
            />
          </div>
```
改为：
```tsx
        <div className="annotatorBody">
          <div className="annotatorImage">
            <ImageViewer
              ref={ivRef}
              src={imageUrl(suiteId, testCase.relative_image_path)}
              alt={testCase.relative_image_path}
              hasPrev={currentIndex > 0}
              hasNext={currentIndex >= 0 && currentIndex < caseList.length - 1}
              onPrev={() => void onRequestNavigate(-1)}
              onNext={() => void onRequestNavigate(1)}
            />
          </div>
```

- [ ] **Step 6: 跟踪 dirty 状态**

把：
```tsx
              <VariableBuilder
                value={variablesJson}
                onChange={setVariablesJson}
                knownKeys={suiteDefinedVarKeys}
                suiteVarHints={suiteVarHints}
              />
```
改为：
```tsx
              <VariableBuilder
                value={variablesJson}
                onChange={(v) => {
                  isInternalChangeRef.current = true
                  onDirtyChange?.(true)
                  setVariablesJson(v)
                }}
                knownKeys={suiteDefinedVarKeys}
                suiteVarHints={suiteVarHints}
              />
```

把：
```tsx
              <label>
                <span className="muted" style={{ fontSize: 12 }}>主图相对路径</span>
                <input
                  value={relativePath}
                  onChange={(e) => setRelativePath(e.target.value)}
                />
              </label>
```
改为：
```tsx
              <label>
                <span className="muted" style={{ fontSize: 12 }}>主图相对路径</span>
                <input
                  value={relativePath}
                  onChange={(e) => {
                    isInternalChangeRef.current = true
                    onDirtyChange?.(true)
                    setRelativePath(e.target.value)
                  }}
                />
              </label>
```

把：
```tsx
              <AssertionBuilder
                value={assertionsJson || '{"rules":[]}'}
                onChange={(next) => {
                  // 用户清空/全部删除规则时，保持空串表示"沿用测试集默认"
                  const r = safeParseOrNull(next)
                  if (!r || (Array.isArray(r.rules) && r.rules.length === 0)) {
                    setAssertionsJson('')
                  } else {
                    setAssertionsJson(next)
                  }
                }}
                schemaFields={schemaFields}
                varKeys={varKeys}
              />
```
改为：
```tsx
              <AssertionBuilder
                value={assertionsJson || '{"rules":[]}'}
                onChange={(next) => {
                  isInternalChangeRef.current = true
                  onDirtyChange?.(true)
                  // 用户清空/全部删除规则时，保持空串表示"沿用测试集默认"
                  const r = safeParseOrNull(next)
                  if (!r || (Array.isArray(r.rules) && r.rules.length === 0)) {
                    setAssertionsJson('')
                  } else {
                    setAssertionsJson(next)
                  }
                }}
                schemaFields={schemaFields}
                varKeys={varKeys}
              />
```

- [ ] **Step 7: 在保存成功后清 dirty**

把：
```tsx
  async function handleSave() {
    setSaving(true)
    setError(null)
    try {
      // 保存前检查：如果用例变量缺少测试集声明的变量，自动补充默认值
      const suiteDefaults = buildDefaultVariablesJson(suiteDefinedVarKeys, suiteVarHints)
      const merged = mergeVariablesJson(variablesJson, suiteDefaults)
      await onSave({
        relative_image_path: relativePath,
        variables_json: merged || '{}',
        assertions_override_json: assertionsJson,
      })
      onClose()
    } catch (e) {
      setError((e as Error).message || '保存失败')
    } finally {
      setSaving(false)
    }
  }
```
改为：
```tsx
  async function handleSave() {
    setSaving(true)
    setError(null)
    try {
      // 保存前检查：如果用例变量缺少测试集声明的变量，自动补充默认值
      const suiteDefaults = buildDefaultVariablesJson(suiteDefinedVarKeys, suiteVarHints)
      const merged = mergeVariablesJson(variablesJson, suiteDefaults)
      await onSave({
        relative_image_path: relativePath,
        variables_json: merged || '{}',
        assertions_override_json: assertionsJson,
      })
      // 保存成功 -> 清 dirty
      isInternalChangeRef.current = false
      onDirtyChange?.(false)
      lastSyncedCaseIdRef.current = testCase.id
      onClose()
    } catch (e) {
      setError((e as Error).message || '保存失败')
    } finally {
      setSaving(false)
    }
  }
```

- [ ] **Step 8: 类型检查**

Run: `cd web && npx tsc -b`
Expected: 0 errors。

- [ ] **Step 9: 提交**

```bash
git add web/src/components/CaseAnnotator.tsx
git commit -m "feat(web): CaseAnnotator 接入 ImageViewer，暴露 dirty 与翻页回调"
```

---

### Task 5: `App.tsx` 新增 `navigateCase` + 透传 props

**Files:**
- Modify: `web/src/App.tsx`

- [ ] **Step 1: 在 `annotatorCase` useMemo 之后新增 dirty ref 与 `navigateCase` 函数**

把 `App.tsx` 中：
```tsx
  /** 当前打开大图标注弹窗的用例对象 */
  const annotatorCase = useMemo(
    () => (annotatorCaseId != null ? cases.find((c) => c.id === annotatorCaseId) ?? null : null),
    [annotatorCaseId, cases],
  )
```
替换为：
```tsx
  /** 当前打开大图标注弹窗的用例对象 */
  const annotatorCase = useMemo(
    () => (annotatorCaseId != null ? cases.find((c) => c.id === annotatorCaseId) ?? null : null),
    [annotatorCaseId, cases],
  )

  /** annotatorCase 在当前可见 cases 中的下标；找不到时为 -1 */
  const annotatorIndex = useMemo(
    () => (annotatorCaseId != null ? cases.findIndex((c) => c.id === annotatorCaseId) : -1),
    [annotatorCaseId, cases],
  )

  /** 大图标注草稿是否改动；翻页前若 dirty 自动保存 */
  const annotatorDirtyRef = useRef(false)

  /** 翻页：方向 -1 / 1。父级在翻页前自动保存当前草稿 */
  const navigateCase = useCallback(
    async (direction: -1 | 1) => {
      if (annotatorCaseId == null || dataSuiteId == null) return
      const idx = cases.findIndex((c) => c.id === annotatorCaseId)
      if (idx < 0) return
      const nextIdx = idx + direction
      if (nextIdx < 0 || nextIdx >= cases.length) return

      // 1) 草稿 dirty -> 自动保存当前用例
      if (annotatorDirtyRef.current) {
        try {
          props.onError(null)
          const target = cases[idx]
          await patchJson(`/api/test-cases/${target.id}`, {
            relative_image_path: target.relative_image_path,
            // 草稿保存在 CaseAnnotator 内部；这里仅在用户在父级另外改动时兜底：
            // 实际草稿由 CaseAnnotator 内部 onSave 回调处理（见 onSave 包裹）
          })
        } catch (e) {
          props.onError((e as Error).message)
          return
        }
      }

      // 2) 切换到目标用例
      const next = cases[nextIdx]
      setAnnotatorCaseId(next.id)
    },
    [annotatorCaseId, cases, dataSuiteId, props],
  )
```

并在文件顶部确认已 `import { useCallback, useMemo, useRef, useState } from 'react'`（已存在则无需改；缺则补）。

- [ ] **Step 2: 修改 `<CaseAnnotator>` 调用处，传入新 props**

把：
```tsx
      {annotatorCase && dataSuiteId != null ? (
        <CaseAnnotator
          open={annotatorCaseId != null}
          suiteId={dataSuiteId}
          testCase={annotatorCase}
          schemaFields={assertionFieldOptions}
          suiteDefinedVarKeys={suiteDefinedVarKeys}
          suiteVarHints={suiteVarValueHints}
          onClose={() => setAnnotatorCaseId(null)}
          onSave={async (payload) => {
            props.onError(null)
            try {
              await patchJson(`/api/test-cases/${annotatorCase.id}`, {
                relative_image_path: payload.relative_image_path,
                variables_json: payload.variables_json || '{}',
                assertions_override_json: payload.assertions_override_json.trim()
                  ? payload.assertions_override_json
                  : null,
              })
              await refreshCases(dataSuiteId)
            } catch (e) {
              props.onError((e as Error).message)
              throw e
            }
          }}
        />
      ) : null}
```
替换为：
```tsx
      {annotatorCase && dataSuiteId != null ? (
        <CaseAnnotator
          open={annotatorCaseId != null}
          suiteId={dataSuiteId}
          testCase={annotatorCase}
          schemaFields={assertionFieldOptions}
          suiteDefinedVarKeys={suiteDefinedVarKeys}
          suiteVarHints={suiteVarValueHints}
          caseList={cases}
          currentIndex={annotatorIndex}
          onRequestNavigate={(d) => void navigateCase(d)}
          onDirtyChange={(dirty) => {
            annotatorDirtyRef.current = dirty
          }}
          onClose={() => setAnnotatorCaseId(null)}
          onSave={async (payload) => {
            props.onError(null)
            try {
              await patchJson(`/api/test-cases/${annotatorCase.id}`, {
                relative_image_path: payload.relative_image_path,
                variables_json: payload.variables_json || '{}',
                assertions_override_json: payload.assertions_override_json.trim()
                  ? payload.assertions_override_json
                  : null,
              })
              await refreshCases(dataSuiteId)
            } catch (e) {
              props.onError((e as Error).message)
              throw e
            }
          }}
        />
      ) : null}
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `cd web && npx tsc -b && npx vite build`
Expected: 0 errors, build 成功。

- [ ] **Step 4: 提交**

```bash
git add web/src/App.tsx
git commit -m "feat(web): App 注入 navigateCase 与大图标注 props"
```

---

### Task 6: 让 `CaseAnnotator` 的 onSave 真正能由父级在翻页时复用

**Files:**
- Modify: `web/src/App.tsx`

**为什么需要这一步：** 任务 5 的 `navigateCase` 在草稿 dirty 时通过父级 `onSave` 路径不可直接拿（草稿状态在 `CaseAnnotator` 内部）。最简洁的做法：把"先保存再切"完全交给 `CaseAnnotator` —— 由 `CaseAnnotator` 暴露一个 `onRequestNavigate` 钩子，在父级调它时内部先 `await handleSave()` 再 `props.onRequestNavigate()`。

- [ ] **Step 1: 调整 `CaseAnnotator` 的 props 语义**

`web/src/components/CaseAnnotator.tsx` 顶部 props 描述不变（`onRequestNavigate` 仍是父级钩子），但实现层多加一个内部协调器：父级不再直接 `navigateCase`，而是把方向传给 `CaseAnnotator`，由其内部决定"先保存再通知父级切"。

把 `CaseAnnotator` 内 `onPrev` / `onNext` 的两处调用：

```tsx
              onPrev={() => void onRequestNavigate(-1)}
              onNext={() => void onRequestNavigate(1)}
```

改为：

```tsx
              onPrev={() => void handleNavigate(-1)}
              onNext={() => void handleNavigate(1)}
```

并在 `CaseAnnotator` 函数体内（紧挨 `handleSave` 之后）新增：

```tsx
  /** 翻页：先尝试保存草稿，再通知父级切换 */
  async function handleNavigate(direction: -1 | 1) {
    try {
      if (isInternalChangeRef.current) {
        // 草稿未保存：先保存当前用例，成功后再切
        const suiteDefaults = buildDefaultVariablesJson(suiteDefinedVarKeys, suiteVarHints)
        const merged = mergeVariablesJson(variablesJson, suiteDefaults)
        await onSave({
          relative_image_path: relativePath,
          variables_json: merged || '{}',
          assertions_override_json: assertionsJson,
        })
        isInternalChangeRef.current = false
        onDirtyChange?.(false)
      }
      await onRequestNavigate(direction)
    } catch (e) {
      setError((e as Error).message || '翻页失败')
    }
  }
```

- [ ] **Step 2: 简化父级 `navigateCase`（不再在父级判 dirty）**

把 `App.tsx` 中 Task 5 添加的 `navigateCase` 替换为：

```tsx
  /** 翻页：父级仅负责计算下一个 id 并切换。草稿保存由 CaseAnnotator 内部协调。 */
  const navigateCase = useCallback(
    (direction: -1 | 1) => {
      if (annotatorCaseId == null) return
      const idx = cases.findIndex((c) => c.id === annotatorCaseId)
      if (idx < 0) return
      const nextIdx = idx + direction
      if (nextIdx < 0 || nextIdx >= cases.length) return
      const next = cases[nextIdx]
      setAnnotatorCaseId(next.id)
    },
    [annotatorCaseId, cases],
  )
```

- [ ] **Step 3: 类型检查**

Run: `cd web && npx tsc -b`
Expected: 0 errors。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/CaseAnnotator.tsx web/src/App.tsx
git commit -m "refactor(web): 翻页时草稿保存由 CaseAnnotator 内部协调"
```

---

### Task 7: 手工验收（按 spec §6）

**Files:** 无代码改动。

- [ ] **Step 1: 启动 dev server**

Run: `cd web && npx vite`
Expected: Vite 启动并打印 `Local: http://localhost:5173/`。

- [ ] **Step 2: 浏览器手工验证清单**

在浏览器打开数据页 → 点击某行的「大图标注」按钮，按 spec §6 逐条验证：

1. ✅ 打开大图标注 → 图片默认「适应」
2. ✅ 工具条 `+`/`−` → 倍率正确显示，最大 800%，最小 25%
3. ✅ 倍率按钮 → 在 100% / 适应 切换
4. ✅ 滚轮（图片上）→ 缩放，光标下像素不动；表单上滚轮 → 滚动表单
5. ✅ 放大后拖动 → 平移
6. ✅ 翻页（鼠标移到图片上 → `‹` / `›`）→ 缩放重置为适应
7. ✅ 翻页前 dirty → 自动保存后切换；保存失败 → 红字提示，不切换
8. ✅ 网络断开翻页 → 错误占位 + 重试按钮
9. ✅ Esc / 遮罩 / 取消 → 关闭弹窗不自动保存
10. ✅ 回归：变量/断言可视化编辑器仍可用

- [ ] **Step 3: 关闭 dev server**

浏览器中按 `Ctrl+C` 关闭 Vite。

---

## 验证一致性说明

- 类型 / 方法名：
  - `ImageViewerHandle.resetView` — Task 1 定义、Task 2/4 使用
  - `CaseAnnotator.onRequestNavigate` — Task 4 定义、Task 5/6 使用
  - `CaseAnnotator.onDirtyChange` — Task 4 定义、Task 5/6 使用
  - `App.navigateCase` — Task 5 定义、Task 6 简化
- 文件路径：
  - `web/src/components/ImageViewer.tsx` — Task 1 创建
  - `web/src/components/CaseAnnotator.tsx` — Task 4/6 改
  - `web/src/App.css` — Task 3 追加
  - `web/src/App.tsx` — Task 5/6 改
- 行为对齐 spec：
  - 缩放范围 0.25–8，步进 1.25：Task 1 / Task 2 显式常量
  - 滚轮仅图片区：Task 2 在 `stageRef` 上 `onWheel` + `preventDefault`
  - 翻页按"当前可见列表顺序"：Task 4 / Task 5 透传 `cases` 而非重排
  - 翻页前自动保存：Task 6 在 `CaseAnnotator.handleNavigate` 中先 `await onSave`
  - 翻页/打开/关闭重置为适应：Task 1/4 在 `useEffect` 调 `resetView`
  - 加载/失败：Task 1 status + Task 1 `retryToken` 触发 src 变更
